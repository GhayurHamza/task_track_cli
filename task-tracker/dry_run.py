import os


def create_task(command,param,descr):
    #if "all_task.json" dne then create and add task with properties to it
    # task {id:1, descr: "", status: "to-do", createdAt: "", updatedAt: ""}
     
    pass